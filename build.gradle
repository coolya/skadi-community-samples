

task createZip(type: Zip) {
    archiveFileName = "community-samples.zip"
    destinationDirectory = file("$buildDir/dist")
    from("kernelf-demo") {
        into("kernelf-demo")
    }
    from("heavymeta.tv") {
        into("heavymeta.tv")
    }
    from("mpsintrocourse") {
        into("mpsintrocourse")
    }
}
